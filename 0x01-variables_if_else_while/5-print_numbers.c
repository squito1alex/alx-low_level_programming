#include <stdio.h>
#include <unistd.h>
/**
 * main - Entry point
 * Description: prints all single digit numbers of base 10 from 0
 * Return: 0 (success)
 */
int main(void)
{
	int k;
	for (k = 0; k < 10; k++)
	{
		printf("%d", k);
	}
	putchar('\n')
		return (0)
}
