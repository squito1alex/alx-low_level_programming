#include <stdio.h>
/**
 * main-Serves as the empty point for the program
 *
 * Return:By default returns zero of no errors.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
